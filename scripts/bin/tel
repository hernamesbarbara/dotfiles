#!/usr/bin/env python
# -*- coding: utf-8 -*- 
"""tel - strip punctuation from telephone numbers
"""
import os
import sys
from phonenumbers import normalize_digits_only

if __name__ == "__main__":
    txt = u" ".join(sys.argv[1:])
    print >> sys.stdout, normalize_digits_only(txt)
